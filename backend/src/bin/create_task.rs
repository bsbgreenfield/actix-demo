use backend::*;

fn main() {
    let title = "new task2";
    let body = "this is the body22222!";

    //let pool = &mut get_pool();
    //let conn = pool.get().await.expect("couldnt get a pool");
    //let new_task = create_new_task(conn, title, Some(body));

    //println!("title = {}", new_task.title);
    //println!("body = {}", new_task.body.unwrap());
}
